/// Local plugin: native Samsung Health Data SDK integration.
///
/// All access goes through the existing `MethodChannel('com.countit.app/samsung_health')`
/// in `lib/src/data/datasources/local/samsung_health_datasource.dart`. This file exists
/// only so `flutter pub` recognizes the plugin and adds it to GeneratedPluginRegistrant.
library samsung_health;
